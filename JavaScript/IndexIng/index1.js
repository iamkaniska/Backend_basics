let num=[1,2,3,4,5];
let res=num.indexOf(7);
console.log(res);

/* if it shows -1 in result that means this element is not exist in this array or if it returns anyof the number except -1 that means this number is exist in this array, and the number that returns is the index of that specific element*/
